[project]
name = "serialbridge"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "opencv-python>=4.12",
    "psycopg[binary]>=3.3.4",
    "pyserial>=3.5",
]

[tool.uv]
package = false
